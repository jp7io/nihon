// @ts-check

/**
 * @typedef {import('./regions').Name} Name
 * @typedef {import('./regions').Region} Region
 * @typedef {import('./regions').Prefecture} Prefecture
 * @typedef {import('./regions').City} City
 * @typedef {import('./tokyo').Municipality} Municipality
 * @typedef {Region | Prefecture | City | Municipality} LocationData
 */

export { };
